---
---

CI only, no package changes: interim beta builds are now numbered from the version the pending
changesets are heading for (e.g. `1.1.0-beta.<stamp>`) instead of the version that was last
released (`1.0.0-beta.<stamp>`, which sorted below the release it followed).
